(INICIO)
        @10   // carga A con 10
        D=A   // D = 10
        @0    // A = 0
        M=D   // M[0] = 10
        @1
        D=A
        @INICIO  // carga A con 0
        0;JMP    // salta a 0